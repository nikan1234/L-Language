class Base {}

class Derived extends Base {
  public a;
  private b;
  protected c;
}

function bar(c) {
  l := c + 8;
}

function foo(y){
 g := []

 r := y;
 k := r + 6;
 bar(k);
}

foo(5);
v := foo(10);

a := 5;
b := (a + 1) * (3 + 1);





